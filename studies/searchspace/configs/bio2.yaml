function: 'olympus.baselines.bio_pMHC_task2.main'

objective: 'validation_aac'

variables:
    random_state: 1
    bootstrap_seed: 1

fidelity: null

defaults: {}

space:
    hidden_layer_sizes: 'loguniform(20, 400)'
    alpha: 'uniform(0, 1)'
